<div class="main-container">
  <div class="picture-container">
  </div>

  <div class="mygrid">


  <div>
  <h2>Materials needed</h2>
  <p><%= @creation.name %></p>
  <p><%= @creation.time %></p>


  <div style="margin-left: 90%">
    <% if !@fav_exist %>
       <%= link_to "FAV",
            creation_favoris_path(@creation.id),
            method: :post,
            class: "btn btn-gradient"%>
      <% else %>
      <%= link_to "NoFAV",
            favori_path(@fav[0]),
            method: :delete,
            class: "btn btn-gradient"%>
            <% end %>
  </div>


    <div>
      <h2>Materials needed</h2>
      <p><%= @creation.name %></p>
    </div>

    <div>
      <h2>Materials needed</h2>
      <p><%= @creation.name %></p>
    </div>

    <div>
      <h2>Materials needed</h2>
      <p><%= @creation.name %></p>
    </div>

    <div>
      <h2>Materials needed</h2>
      <p><%= @creation.name %></p>
    </div>

    <div>
      <h2>Materials needed</h2>
      <p><%= @creation.name %></p>
    </div>

  </div>

  <div>
    <%= link_to "My PDF", asset_path(@creation.patron_link), target: :_blank %>
  </div>

  <div>
    <% @creations.each do |creation| %>
      <% if creation.id != @creation.id %>
        <%# alors ça ça devra être une card de creation %>
        <%= creation.name %>
        <br>
      <% end %>
    <% end %>
  </div>

  <%= link_to "Back to Index", creations_path(:query_long => params[:query_long], :query_larg => params[:query_larg]) %>
</div>
